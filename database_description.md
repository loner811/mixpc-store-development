# База данных MixPC Store - Описание структуры

## 📋 Общая информация
База данных интернет-магазина компьютерной техники с функциями каталога, корзины, заказов, сборок ПК и административной панели.

Всего таблиц: **12**

---

## 👥 1. Таблица `users` (Пользователи)
**Назначение:** Хранит информацию о зарегистрированных пользователях и администраторах системы.

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | INT | Уникальный идентификатор пользователя (автоинкремент) |
| `email` | VARCHAR(255) | Email пользователя (уникальный) |
| `username` | VARCHAR(100) | Имя пользователя (уникальное) |
| `password_hash` | VARCHAR(255) | Хэш пароля |
| `role` | VARCHAR(20) | Роль: 'user' (обычный) или 'admin' (администратор) |
| `created_at` | TIMESTAMP | Дата и время регистрации |

**Связи:**
- → `orders` (один пользователь → много заказов)
- → `pc_builds` (один пользователь → много сборок ПК)
- → `favorites` (один пользователь → много избранных товаров)
- → `cart_items` (один пользователь → много товаров в корзине)

---

## 📂 2. Таблица `categories` (Категории товаров)
**Назначение:** Справочник категорий товаров для навигации и фильтрации в каталоге.

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | INT | Уникальный идентификатор категории |
| `name` | VARCHAR(100) | Название категории (например, "Процессоры", "Видеокарты") |
| `slug` | VARCHAR(100) | URL-friendly идентификатор (например, "processors", "videocards") |
| `icon` | VARCHAR(50) | Название иконки для отображения в интерфейсе |
| `created_at` | TIMESTAMP | Дата создания категории |

**Примеры категорий:**
- Процессоры (processors)
- Видеокарты (videocards)
- Материнские платы (motherboards)
- Оперативная память (ram)
- Накопители SSD (ssd)
- Блоки питания (psu)
- Корпуса (cases)
- Кулеры (coolers)
- Мониторы (monitors)
- Клавиатуры (keyboards)
- Компьютерные мыши (mice)

**Связи:**
- → `products` (одна категория → много товаров)

---

## 🛍️ 3. Таблица `products` (Товары)
**Назначение:** Основная таблица каталога товаров с характеристиками и ценами.

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | INT | Уникальный идентификатор товара |
| `category_id` | INT | ID категории товара (связь с `categories`) |
| `name` | VARCHAR(255) | Название товара |
| `description` | TEXT | Подробное описание товара |
| `price` | DECIMAL(10,2) | Цена товара в рублях |
| `brand` | VARCHAR(100) | Производитель (AMD, Intel, NVIDIA, Kingston и т.д.) |
| `image_filename` | VARCHAR(255) | Имя файла изображения товара |
| `specs` | JSON | Технические характеристики в формате JSON |
| `is_popular` | TINYINT(1) | Флаг популярного товара (0/1) |
| `is_featured` | TINYINT(1) | Флаг рекомендованного товара (0/1) |
| `stock_quantity` | INT | Количество на складе |
| `stock_status` | VARCHAR(20) | Статус наличия: 'in_stock', 'out_of_stock', 'pre_order' |
| `created_at` | TIMESTAMP | Дата добавления товара |
| `updated_at` | TIMESTAMP | Дата последнего обновления |

**Связи:**
- ← `categories` (принадлежит категории)
- → `order_items` (товар в заказах)
- → `product_specifications` (детальные характеристики)
- → `pc_build_items` (товар в сборках ПК)
- → `favorites` (в избранном пользователей)
- → `cart_items` (в корзинах пользователей)

---

## 📦 4. Таблица `orders` (Заказы)
**Назначение:** Хранит информацию о заказах клиентов.

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | INT | Уникальный номер заказа |
| `user_id` | INT | ID пользователя (если зарегистрирован) |
| `full_name` | VARCHAR(255) | ФИО заказчика |
| `email` | VARCHAR(255) | Email для связи |
| `phone` | VARCHAR(50) | Телефон для связи |
| `delivery_type` | VARCHAR(20) | Тип доставки: 'delivery' (доставка) или 'pickup' (самовывоз) |
| `delivery_address` | TEXT | Адрес доставки (если выбрана доставка) |
| `total_amount` | DECIMAL(10,2) | Общая сумма заказа в рублях |
| `status` | VARCHAR(20) | Статус: 'pending' (новый), 'completed' (выполнен), 'cancelled' (отменён) |
| `created_at` | TIMESTAMP | Дата и время создания заказа |
| `updated_at` | TIMESTAMP | Дата последнего изменения статуса |

**Связи:**
- ← `users` (заказ принадлежит пользователю)
- → `order_items` (один заказ → много товаров)

---

## 🛒 5. Таблица `order_items` (Товары в заказе)
**Назначение:** Хранит список товаров в каждом заказе с их ценами на момент покупки.

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | INT | Уникальный идентификатор записи |
| `order_id` | INT | ID заказа (связь с `orders`) |
| `product_id` | INT | ID товара (связь с `products`) |
| `product_name` | VARCHAR(255) | Название товара (зафиксировано на момент заказа) |
| `product_price` | DECIMAL(10,2) | Цена товара на момент заказа |
| `quantity` | INT | Количество единиц товара |
| `created_at` | TIMESTAMP | Дата добавления в заказ |

**Зачем дублируются название и цена?**  
Чтобы сохранить историю: если товар потом переименуют или изменят цену, в заказе останутся данные на момент покупки.

**Связи:**
- ← `orders` (принадлежит заказу)
- ← `products` (ссылка на товар)

---

## 📝 6. Таблица `product_specifications` (Характеристики товаров)
**Назначение:** Детальные технические характеристики товаров в виде пар "ключ-значение".

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | INT | Уникальный идентификатор характеристики |
| `product_id` | INT | ID товара (связь с `products`) |
| `spec_key` | VARCHAR(100) | Название характеристики (например, "Частота", "Память", "TDP") |
| `spec_value` | TEXT | Значение характеристики (например, "5.8 GHz", "32 GB", "125W") |
| `created_at` | TIMESTAMP | Дата добавления характеристики |

**Примеры характеристик для процессора:**
- `spec_key: "Ядра"` → `spec_value: "16"`
- `spec_key: "Потоки"` → `spec_value: "32"`
- `spec_key: "Частота"` → `spec_value: "4.5 GHz"`

**Связи:**
- ← `products` (характеристики принадлежат товару)

---

## 🖥️ 7. Таблица `pc_builds` (Сборки ПК)
**Назначение:** Хранит пользовательские конфигурации сборок компьютеров.

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | INT | Уникальный идентификатор сборки |
| `user_id` | INT | ID пользователя-владельца сборки |
| `build_name` | VARCHAR(255) | Название сборки (например, "Игровая сборка 2024") |
| `total_price` | DECIMAL(10,2) | Общая стоимость всех компонентов |
| `created_at` | TIMESTAMP | Дата создания сборки |
| `updated_at` | TIMESTAMP | Дата последнего изменения |

**Связи:**
- ← `users` (сборка принадлежит пользователю)
- → `pc_build_items` (одна сборка → много компонентов)

---

## 🔧 8. Таблица `pc_build_items` (Компоненты сборки)
**Назначение:** Список товаров, входящих в конкретную сборку ПК.

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | INT | Уникальный идентификатор записи |
| `build_id` | INT | ID сборки (связь с `pc_builds`) |
| `product_id` | INT | ID товара (связь с `products`) |
| `component_type` | VARCHAR(50) | Тип компонента: 'cpu', 'gpu', 'ram', 'motherboard', 'ssd', 'psu', 'case', 'cooler' |
| `created_at` | TIMESTAMP | Дата добавления компонента |

**Пример сборки:**
- `component_type: "cpu"` → Intel Core i9-13900K
- `component_type: "gpu"` → RTX 4090
- `component_type: "ram"` → Kingston Fury 32GB DDR5

**Связи:**
- ← `pc_builds` (компоненты принадлежат сборке)
- ← `products` (ссылка на товар)

---

## ⭐ 9. Таблица `favorites` (Избранное)
**Назначение:** Хранит избранные товары пользователей для быстрого доступа.

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | INT | Уникальный идентификатор записи |
| `user_id` | INT | ID пользователя |
| `product_id` | INT | ID товара |
| `created_at` | TIMESTAMP | Дата добавления в избранное |

**Особенность:** Уникальная пара `(user_id, product_id)` — один пользователь не может добавить один товар дважды.

**Связи:**
- ← `users` (избранное принадлежит пользователю)
- ← `products` (ссылка на товар)

---

## 🛒 10. Таблица `cart_items` (Корзина)
**Назначение:** Временное хранилище товаров в корзине до оформления заказа.

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | INT | Уникальный идентификатор записи |
| `user_id` | INT | ID пользователя |
| `product_id` | INT | ID товара |
| `quantity` | INT | Количество единиц товара |
| `created_at` | TIMESTAMP | Дата добавления в корзину |

**Связи:**
- ← `users` (корзина принадлежит пользователю)
- ← `products` (ссылка на товар)

---

## 💬 11. Таблица `contact_messages` (Сообщения от клиентов)
**Назначение:** Хранит обращения клиентов через форму обратной связи.

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | INT | Уникальный идентификатор сообщения |
| `full_name` | VARCHAR(255) | ФИО отправителя |
| `phone` | VARCHAR(50) | Телефон для связи |
| `email` | VARCHAR(255) | Email для связи |
| `message` | TEXT | Текст сообщения/вопроса |
| `is_read` | TINYINT(1) | Статус прочтения: 0 (не прочитано), 1 (прочитано) |
| `created_at` | TIMESTAMP | Дата и время отправки |

**Связи:**
- → `message_replies` (одно сообщение → много ответов)

---

## 💬 12. Таблица `message_replies` (Ответы на сообщения)
**Назначение:** Хранит ответы администратора на обращения клиентов.

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | INT | Уникальный идентификатор ответа |
| `message_id` | INT | ID исходного сообщения (связь с `contact_messages`) |
| `reply_text` | TEXT | Текст ответа администратора |
| `created_at` | TIMESTAMP | Дата и время ответа |

**Связи:**
- ← `contact_messages` (ответ принадлежит сообщению)

---

## 🔗 Диаграмма связей

```
┌─────────────┐
│   users     │──┐
└─────────────┘  │
                 ├──→ orders ──→ order_items ←── products
                 ├──→ pc_builds ──→ pc_build_items ←── products
                 ├──→ favorites ←── products
                 └──→ cart_items ←── products

┌─────────────┐
│ categories  │──→ products ──→ product_specifications
└─────────────┘

┌──────────────────┐
│contact_messages  │──→ message_replies
└──────────────────┘
```

---

## 📊 Статистика базы данных

| Показатель | Значение |
|------------|----------|
| Всего таблиц | 12 |
| Основных сущностей | 4 (users, categories, products, orders) |
| Связующих таблиц | 8 |
| Внешних ключей | 15 |
| Функциональных модулей | 5 (Каталог, Заказы, Сборки ПК, Корзина, Обратная связь) |

---

## 🎯 Основные бизнес-процессы

1. **Просмотр каталога**: categories → products → product_specifications
2. **Оформление заказа**: cart_items → orders → order_items
3. **Создание сборки ПК**: products → pc_builds → pc_build_items
4. **Обратная связь**: contact_messages → message_replies
5. **Избранное**: users → favorites ← products
